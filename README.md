# Cybersecurity Monitoring Prototype

Полнофункциональный прототип системы мониторинга кибербезопасности с PostgreSQL, JWT-аутентификацией, ролями пользователей и системой алертов.

## Быстрый старт

### 1. Настройка переменных окружения

Создайте файл `.env` из примера:

```bash
cp .env.example .env
```

**ВАЖНО!** Отредактируйте `.env` и установите:
- `JWT_SECRET` - сильный случайный секрет (минимум 32 символа)
- `POSTGRES_PASSWORD` - сильный пароль для БД
- `DATABASE_URL` - обновите с новым паролем

Генерация секретов:
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 2. Запуск проекта

```bash
docker-compose up --build
```

### 3. Доступ к приложению

- 🔗 Backend health: http://localhost:8000/api/health
- 🔐 API Docs: http://localhost:8000/docs
- 📊 Frontend UI: http://localhost:3000

### 4. Создание первого пользователя

Для development можно использовать signup endpoint (если `ALLOW_OPEN_SIGNUP=true`):

```bash
curl -X POST http://localhost:8000/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "AdminPass123",
    "role": "analyst"
  }'
```

**Требования к паролю:**
- Минимум 8 символов
- Минимум 1 заглавная буква
- Минимум 1 строчная буква
- Минимум 1 цифра

## Архитектура

- **Backend**: FastAPI + PostgreSQL + SQLAlchemy
- **Frontend**: React + TypeScript + Vite
- **База данных**: PostgreSQL (автоматически создаётся при первом запуске)

## Backend API

### Аутентификация

- `POST /api/auth/signup` — регистрация пользователя (для прототипа открыта)
  ```json
  {
    "username": "analyst",
    "password": "secret",
    "role": "analyst"  // viewer | analyst | admin
  }
  ```
- `POST /api/auth/token` — получение JWT токена (OAuth2 password flow)
  ```bash
  curl -X POST http://localhost:8000/api/auth/token \
    -H "Content-Type: application/x-www-form-urlencoded" \
    -d "username=analyst&password=secret"
  ```
- `GET /api/auth/me` — текущий пользователь (требует JWT)

**Роли:**
- `viewer` — только просмотр событий и алертов
- `analyst` — может обновлять алерты и управлять правилами
- `admin` — полный доступ, включая удаление правил

### События (Events)

- `GET /api/events/` — список событий (фильтры: `severity`, `category`, `source`, пагинация: `offset`, `limit`)
- `GET /api/events/paged` — то же, но с метаданными пагинации:
  ```json
  {
    "items": [ /* SecurityEvent[] */ ],
    "total": 123,
    "offset": 0,
    "limit": 50
  }
  ```
- `GET /api/events/summary` — агрегированная сводка:
  - `total` — всего событий
  - `by_severity` — количество по уровням важности
  - `by_category` — количество по категориям
  - `last_event_at` — время последнего события
- `POST /api/events/evaluate-all` — ручная оценка всех событий против правил алертов

### Алерты (Alerts)

- `GET /api/alerts/` — список алертов (фильтры: `status`, `rule_id`, `assigned_to`, пагинация)
- `GET /api/alerts/{alert_id}` — детали алерта
- `PATCH /api/alerts/{alert_id}` — обновление алерта (status, assigned_to, notes) — требует `analyst` или `admin`

### Правила алертов (Alert Rules)

- `GET /api/alerts/rules/` — список правил
- `POST /api/alerts/rules/` — создание правила — требует `analyst` или `admin`
  ```json
  {
    "name": "High Severity Network Events",
    "description": "Alert on high/critical network events",
    "severity_filter": "high",
    "category_filter": "network",
    "source_filter": null,
    "is_active": true
  }
  ```
- `GET /api/alerts/rules/{rule_id}` — детали правила
- `PATCH /api/alerts/rules/{rule_id}` — обновление правила — требует `analyst` или `admin`
- `DELETE /api/alerts/rules/{rule_id}` — удаление правила — требует `admin`

### API key (опциональная защита)

Если выставить переменную окружения `API_KEY` для backend-контейнера:
- все endpoints `/api/events*` и `/api/alerts*` требуют заголовок: `X-API-Key: <ваш ключ>`
- если `API_KEY` не задан, защита отключена (удобно для локальной разработки)

**Примечание:** JWT-аутентификация работает независимо от API key. Для UI рекомендуется использовать JWT, для машинной интеграции — API key.

## Frontend

### Вкладка Events

- Сводные карточки (total, high/critical, количество категорий, время последнего события)
- Таблица событий с фильтрами и пагинацией
- Автообновление сводки каждые 15 секунд

### Вкладка ATT&CK Matrix

- Упрощённая матрица тактик и техник для визуальной демонстрации

### Вкладка Alerts

- **Активные алерты**: таблица с фильтрацией по статусу, редактирование статуса и назначение аналитика
- **Правила алертов**: создание, редактирование и удаление правил с фильтрами по severity, category, source

## База данных

При первом запуске автоматически:
- Создаются таблицы: `security_events`, `users`, `alert_rules`, `alerts`
- Загружаются начальные события из `data/initial_events.json`
- События автоматически оцениваются против активных правил алертов

## Переменные окружения

Смотрите `.env.example` для полного списка переменных.

### Критически важные (обязательны для production)

- `JWT_SECRET` — **ОБЯЗАТЕЛЬНО**: секрет для подписи JWT токенов (минимум 32 символа)
- `POSTGRES_PASSWORD` — **ОБЯЗАТЕЛЬНО**: пароль для БД
- `DATABASE_URL` — **ОБЯЗАТЕЛЬНО**: строка подключения к PostgreSQL

### Безопасность

- `ALLOW_OPEN_SIGNUP` — разрешить открытую регистрацию (по умолчанию: `false`)
- `ALLOWED_ORIGINS` — разрешенные CORS origins (по умолчанию: `http://localhost:3000`)
- `API_KEY` — опциональный ключ для machine-to-machine аутентификации

### Прочие

- `EVENTS_FILE` — путь к JSON-файлу с начальными событиями (по умолчанию: `/app/data/initial_events.json`)

## Безопасность

Проект включает следующие меры безопасности:

✅ **Аутентификация**: JWT токены с обязательным секретом
✅ **Валидация**: Строгая валидация паролей и входных данных
✅ **Rate Limiting**: Защита от брутфорса (5 попыток/минуту на login)
✅ **SQL Injection**: Параметризованные запросы и ORM
✅ **CORS**: Настраиваемый список разрешенных origins
✅ **Foreign Keys**: Ссылочная целостность в БД
✅ **Password Hashing**: Bcrypt для хэширования паролей
✅ **Role-Based Access**: Разграничение прав доступа

Подробнее см. [SECURITY.md](./SECURITY.md)

## Тестирование

### Backend тесты

```bash
cd backend
pip install -r requirements.txt
pytest tests/ -v
```

### Проверка зависимостей

```bash
pip install safety
safety check -r backend/requirements.txt
```

## Разработка

### Backend

```bash
cd backend
pip install -r requirements.txt

# Установите переменные окружения
export JWT_SECRET="dev-secret-key-at-least-32-characters-long"
export DATABASE_URL="postgresql://cybersec:cybersec@localhost:5432/cybersec"
export ALLOW_OPEN_SIGNUP="true"

uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

## Production Deployment

### Чеклист безопасности

Перед деплоем в production:

- [ ] Установлен сильный `JWT_SECRET` (минимум 32 символа)
- [ ] Установлен сильный `POSTGRES_PASSWORD`
- [ ] `ALLOW_OPEN_SIGNUP=false` (отключена открытая регистрация)
- [ ] `ALLOWED_ORIGINS` настроен на ваш домен
- [ ] Используется HTTPS
- [ ] Порт БД (5432) не открыт наружу
- [ ] Настроен firewall
- [ ] Настроено логирование и мониторинг
- [ ] Проверены зависимости на уязвимости

### Рекомендуемая архитектура

```
Internet
   ↓
[Load Balancer / CDN]
   ↓
[Reverse Proxy (nginx/traefik)]
   ↓
[Frontend Container] ← [Backend Container] ← [PostgreSQL Container]
                           ↓
                    [Data Volume]
```

## Changelog

### Version 2.0 (Текущая)

**Улучшения безопасности:**
- ✅ Обязательный JWT_SECRET через переменные окружения
- ✅ Строгая валидация паролей (8+ символов, uppercase, lowercase, цифры)
- ✅ Rate limiting на login endpoint (5 попыток/минуту)
- ✅ Защита от SQL инъекций через параметризацию
- ✅ Настраиваемый CORS
- ✅ Foreign Keys и индексы в БД
- ✅ Логирование попыток входа

**Инфраструктура:**
- ✅ Healthchecks для всех сервисов
- ✅ Фиксированные версии зависимостей
- ✅ Базовые unit-тесты (pytest)
- ✅ Документация по безопасности (SECURITY.md)

**Исправлены уязвимости:**
- 🔒 Отключена открытая регистрация по умолчанию
- 🔒 Валидация ролей при регистрации
- 🔒 Исправлена SQL инъекция в фильтрах
- 🔒 Использование datetime.now(timezone.utc) вместо utcnow()
- 🔒 Ограничение количества событий для предотвращения OOM

## Лицензия

MIT

## Поддержка

По вопросам безопасности см. [SECURITY.md](./SECURITY.md)
