# Changelog

## Version 2.0 - Security Hardening (2025-11-26)

### 🔒 Критические исправления безопасности

#### Аутентификация
- **[CRITICAL]** JWT_SECRET теперь обязательно должен быть установлен через переменные окружения
- **[CRITICAL]** Открытая регистрация отключена по умолчанию (`ALLOW_OPEN_SIGNUP=false`)
- **[HIGH]** Добавлена валидация ролей при регистрации - только viewer и analyst доступны через signup
- **[HIGH]** Строгая валидация паролей:
  - Минимум 8 символов
  - Обязательна минимум 1 заглавная буква
  - Обязательна минимум 1 строчная буква
  - Обязательна минимум 1 цифра
- **[HIGH]** Rate limiting на endpoint `/api/auth/token` (5 попыток в минуту)
- **[MEDIUM]** Логирование неудачных попыток входа с IP адресом
- **[MEDIUM]** Проверка is_active перед выдачей токена

#### SQL Injection
- **[HIGH]** Исправлена SQL инъекция в фильтре `source` (event_service.py:26)
  - Было: `like = f"%{source}%"`
  - Стало: `func.concat('%', source, '%')`

#### Входные данные
- **[MEDIUM]** Валидация username через регулярное выражение (только a-z, A-Z, 0-9, -, _)
- **[MEDIUM]** Ограничение длины username (3-50 символов)
- **[MEDIUM]** Валидация ролей через паттерн (viewer|analyst|admin)

### 🛡️ Улучшения безопасности

#### CORS и Network
- Добавлен CORS middleware с настраиваемым списком origins
- Переменная окружения `ALLOWED_ORIGINS` для контроля доступа
- По умолчанию разрешен только `http://localhost:3000`

#### База данных
- Добавлены Foreign Keys с CASCADE delete для:
  - `AlertORM.rule_id` → `alert_rules.id`
  - `AlertORM.event_id` → `security_events.id`
- Добавлены составные индексы:
  - `ix_events_severity_category` для оптимизации фильтрации
  - `ix_events_timestamp_severity` для сортировки по времени
  - `ix_alerts_status_created` для dashboard queries
  - `ix_alerts_rule_event` для уникальности алертов
- Индексы на часто используемые поля:
  - `AlertORM.status`
  - `AlertORM.assigned_to`
  - `AlertRuleORM.is_active`

#### Устаревшие API
- Заменено `datetime.utcnow()` на `datetime.now(timezone.utc)` во всех моделях
- Использование timezone-aware datetime объектов

#### Защита от DoS
- Ограничение загрузки событий в `get_events_summary()` до 100,000 записей
- Предотвращение OOM на больших датасетах

### 📦 Зависимости

#### Backend
- Зафиксированы версии всех зависимостей:
  - fastapi==0.109.0
  - uvicorn[standard]==0.27.0
  - sqlalchemy==2.0.25
  - psycopg2-binary==2.9.9
  - python-jose[cryptography]==3.3.0
  - passlib[bcrypt]==1.7.4
- Добавлены новые зависимости:
  - slowapi==0.1.9 (rate limiting)
  - pydantic==2.5.3 (валидация)
  - pydantic-settings==2.1.0
  - pytest==7.4.4 (тестирование)
  - pytest-asyncio==0.23.3
  - httpx==0.26.0

#### Frontend
- Зафиксированы версии:
  - react==18.2.0
  - react-dom==18.2.0
  - vite==4.5.2
  - typescript==5.3.3
- Добавлены типы:
  - @types/react==18.2.48
  - @types/react-dom==18.2.18

### 🔧 Инфраструктура

#### Docker
- Добавлены healthchecks для всех сервисов
- `depends_on` с условием `service_healthy`
- Обязательные переменные окружения с валидацией (`?` синтаксис)
- Поддержка `.env` файлов

#### Переменные окружения
- Создан `.env.example` с документацией
- Обязательные переменные:
  - `JWT_SECRET` (required)
  - `POSTGRES_PASSWORD` (required)
  - `DATABASE_URL` (required)
- Опциональные переменные:
  - `ALLOW_OPEN_SIGNUP` (default: false)
  - `ALLOWED_ORIGINS` (default: http://localhost:3000)
  - `API_KEY` (optional)

### 🧪 Тестирование

#### Backend Tests
- Добавлен pytest конфигурация (pytest.ini)
- Базовые unit-тесты (backend/tests/test_auth.py):
  - Health endpoint
  - Login (success/failure)
  - Current user
  - Password validation
  - Username validation
  - Role validation

#### Покрытие
- Тесты аутентификации
- Тесты валидации моделей
- Использование in-memory SQLite для тестов

### 📚 Документация

#### Новые файлы
- **SECURITY.md**: Полное руководство по безопасности
  - Реализованные меры
  - Конфигурация для production
  - Генерация секретов
  - Чеклист безопасности
  - Incident response

- **CHANGELOG.md**: История изменений

- **.env.example**: Шаблон переменных окружения с документацией

#### Обновленная документация
- **README.md**:
  - Секция "Быстрый старт" с настройкой переменных
  - Секция "Безопасность" с чеклистом
  - Секция "Тестирование"
  - Секция "Production Deployment"
  - Changelog интегрирован в README

### 🐛 Исправлены баги

- Исправлен потенциальный OOM в `get_events_summary()`
- Исправлена SQL инъекция в фильтрах событий
- Исправлено использование deprecated `datetime.utcnow()`
- Добавлена проверка активности пользователя при входе

### ⚠️ Breaking Changes

1. **JWT_SECRET обязателен**: Приложение не запустится без установленной переменной `JWT_SECRET`
2. **Открытая регистрация отключена**: По умолчанию `ALLOW_OPEN_SIGNUP=false`
3. **Требования к паролям**: Более строгие требования могут сломать существующих пользователей
4. **POSTGRES_PASSWORD обязателен**: Docker Compose требует установки пароля через .env

### 📋 Migration Guide (v1.0 → v2.0)

1. Создайте `.env` файл из `.env.example`
2. Сгенерируйте и установите `JWT_SECRET`:
   ```bash
   python -c "import secrets; print(secrets.token_urlsafe(32))"
   ```
3. Установите `POSTGRES_PASSWORD`
4. Обновите `DATABASE_URL` с новым паролем
5. Для разработки установите `ALLOW_OPEN_SIGNUP=true`
6. Пересоздайте БД (Foreign Keys требуют recreation):
   ```bash
   docker-compose down -v
   docker-compose up --build
   ```
7. Создайте новых пользователей с сильными паролями

### 🎯 TODO для Production

- [ ] Настроить HTTPS
- [ ] Настроить reverse proxy (nginx/traefik)
- [ ] Настроить централизованное логирование
- [ ] Настроить мониторинг и алерты
- [ ] Настроить backup БД
- [ ] Провести security audit
- [ ] Настроить WAF
- [ ] Настроить IDS/IPS

---

## Version 1.0 - Initial Release

- FastAPI backend с PostgreSQL
- React frontend с TypeScript
- JWT аутентификация
- Role-based access control
- Система алертов и правил
- Docker Compose deployment
