# Доступ Claude AI к проекту

## Текущий статус
✅ Репозиторий **публичный** - Claude AI может читать код напрямую

## Способы предоставления доступа

### 1. Прямая ссылка (уже работает)
Просто укажите Claude AI эту ссылку:
```
https://github.com/okidoki888/cybersec-monitoring-prototype
```

### 2. В Cursor IDE
- Откройте проект в Cursor
- Claude автоматически получит доступ ко всем файлам
- Используйте Cmd+K или Cmd+L для взаимодействия с Claude

### 3. GitHub API токен (для автоматизации)
Если нужен программный доступ через API:

1. Создайте Personal Access Token:
   - Перейдите: https://github.com/settings/tokens
   - Generate new token (classic)
   - Права: `public_repo` (для публичных репозиториев)
   - Или `repo` (для приватных)

2. Используйте токен в запросах:
   ```bash
   curl -H "Authorization: token YOUR_TOKEN" \
        https://api.github.com/repos/okidoki888/cybersec-monitoring-prototype
   ```

### 4. Claude Projects (если доступно)
- Создайте проект в Claude AI
- Добавьте URL репозитория
- Claude сможет анализировать весь кодбазу

## Текущий URL репозитория
🔗 https://github.com/okidoki888/cybersec-monitoring-prototype

## Структура проекта
- `backend/` - FastAPI приложение с PostgreSQL
- `frontend/` - React + TypeScript UI
- `docker-compose.yml` - Оркестрация контейнеров
- `README.md` - Полная документация

